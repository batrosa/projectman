# ProjectMan - Система управления проектами

## 🎯 Новые возможности: Автоматический вход

Приложение теперь **автоматически запоминает** вашу учетную запись!

### ✨ Что изменилось?

1. **Автоматический вход**: После первого входа вы больше не вводите email и пароль при следующих посещениях
2. **Выбор роли каждый раз**: Для безопасности роль (Admin/Reader) нужно выбирать при каждом входе
3. **Подтверждение Admin**: Роль администратора всегда требует ввода пароля (301098)

### 🔐 Как это работает?

#### Первый вход:
1. Зарегистрируйтесь или войдите с email и паролем
2. Выберите роль: **Администратор** (с паролем 301098) или **Читатель**
3. Начните работу

#### Последующие посещения:
1. Откройте приложение - вход выполнится автоматически
2. **Выберите роль** (Admin требует пароль 301098)
3. Начинайте работу!

### 🚀 Технические детали

#### Firebase Authentication Persistence
- Использует `LOCAL` persistence
- Сессия сохраняется даже после закрытия браузера
- Автоматическое восстановление при перезагрузке страницы

#### Firestore Structure
```
users/{userId}
  ├─ email: string
  ├─ role: "admin" | "reader" (для статистики)
  └─ lastLogin: timestamp

projects/{projectId}
  ├─ name: string
  ├─ description: string
  └─ createdAt: timestamp

tasks/{taskId}
  ├─ projectId: string
  ├─ title: string
  ├─ assignee: string
  ├─ deadline: string
  ├─ status: "in-progress" | "done"
  └─ createdAt: timestamp
```

### 📋 Развертывание

#### 1. Обновите Firestore Rules
См. файл `FIRESTORE_DEPLOYMENT.md` для инструкций

#### 2. Разверните на Netlify
```bash
# Если используете Netlify CLI
netlify deploy --prod

# Или через веб-интерфейс Netlify
# Просто загрузите файлы через drag-and-drop
```

### 🔄 Выход из системы

Чтобы полностью выйти из аккаунта:
1. Нажмите кнопку **"Выйти"** в боковой панели
2. Вам нужно будет снова ввести email и пароль

**Примечание**: Если вы просто хотите сменить роль, достаточно перезагрузить страницу - вы автоматически войдете и сможете выбрать другую роль.

### 🛡️ Безопасность

- Email/Password аутентификация через Firebase
- Роль Admin требует подтверждения паролем при каждом входе
- Роль не сохраняется - выбирается при каждой сессии
- Правила безопасности ограничивают доступ к данным пользователя
- Пароль администратора: `301098`

### 📱 Поддержка устройств

- ✅ Desktop (Chrome, Firefox, Safari, Edge)
- ✅ Mobile (iOS Safari, Chrome Mobile)
- ✅ Tablet
- ✅ Offline-ready (с Firebase persistence)

### 🐛 Устранение неполадок

**Проблема**: Не могу войти автоматически
- Проверьте, что Firestore Rules развернуты
- Очистите кэш браузера и попробуйте снова
- Проверьте консоль браузера на наличие ошибок

**Проблема**: Приходится каждый раз выбирать роль
- Это нормальное поведение для безопасности
- Роль Admin всегда требует подтверждения паролем
- Если хотите быстрый доступ, используйте роль Reader

---

## 📄 Файлы проекта

- `index.html` - Главная страница
- `style.css` - Стили приложения
- `script.js` - Логика приложения с Firebase интеграцией
- `firestore.rules` - Правила безопасности Firestore
- `firebase.json` - Конфигурация Firebase
- `FIRESTORE_DEPLOYMENT.md` - Инструкции по развертыванию

---

Made with ❤️ using Firebase & Netlify
