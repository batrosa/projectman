# Инструкция по выпуску обновлений

## 🚀 Быстрый чеклист

Когда вы вносите изменения в код, выполните эти шаги:

### 1. Измените номер версии в TWO местах:

**А. В `version-check.js`** (строка 2):
```javascript
const APP_VERSION = "1.9"; // ← Увеличьте на 0.1
```

**Б. В `index.html`** (строки со скриптами):
```html
<script src="/version-check.js?v=1.9"></script>   ← Измените здесь
<script src="/script.js?v=1.9" defer></script>    ← И здесь
```

Также измените версии CSS если они менялись:
```html
<link rel="stylesheet" href="/style.css?v=1.2">           ← Если меняли CSS
<link rel="stylesheet" href="/mobile-additions.css?v=1.4"> ← Если меняли mobile CSS
```

### 2. Загрузите на Netlify ВСЕ файлы:
- ✅ `index.html`
- ✅ `version-check.js` ← ОБЯЗАТЕЛЬНО!
- ✅ `script.js`
- ✅ `style.css` (если меняли)
- ✅ `mobile-additions.css` (если меняли)
- ✅ `netlify.toml`
- ✅ `_headers`

### 3. Проверьте обновление:
1. Откройте сайт
2. F12 → Console
3. Должны увидеть:
   ```
   ✅ Версия актуальна: 1.9
   ```

4. Попросите кого-то из сотрудников обновить страницу (F5)
5. У них должно быть:
   ```
   🔄 Обнаружена новая версия: 1.9 (была: 1.8)
   Принудительное обновление...
   ```
6. Страница автоматически перезагрузится

---

## 🔄 Как это работает

### Автоматическое обновление:

1. **Пользователь открывает сайт**
2. **Браузер загружает `index.html`** (не из кэша, благодаря мета-тегам)
3. **Запускается `version-check.js`** (первым делом!)
4. **Проверка версии:**
   ```javascript
   Есть в localStorage: "1.8"
   Текущая в коде:     "1.9"
   
   Не совпадают! → Принудительная перезагрузка
   ```
5. **`window.location.reload(true)`** → игнорирует весь кэш
6. **Загружает всё заново** с новыми версиями
7. **Сохраняет новую версию** в localStorage

### Почему работает даже со старым кэшем:

- HTML **не кэшируется** (мета-теги `Cache-Control: no-cache`)
- HTML загружается → содержит **новую** версию в `version-check.js`
- Скрипт видит несовпадение → **принудительная перезагрузка**
- При перезагрузке `reload(true)` → **игнорирует кэш**

---

## 📋 Примеры версий

### Версия 1.9 → 2.0 (большое обновление):
```javascript
// version-check.js
const APP_VERSION = "2.0";
```

### Версия 2.0 → 2.1 (небольшое обновление):
```javascript
// version-check.js
const APP_VERSION = "2.1";
```

### Версия 2.1 → 2.1.1 (hotfix):
```javascript
// version-check.js
const APP_VERSION = "2.1.1";
```

**Правило:** Любое изменение версии вызовет обновление у пользователей!

---

## ⚠️ Важные моменты

### ❌ НЕ ЗАБУДЬТЕ:

1. **Изменить версию в обоих местах:**
   - `version-check.js` (сама версия)
   - `index.html` (в URL скриптов)

2. **Загрузить `version-check.js` на Netlify**
   - Без него механизм не работает!

3. **Проверить в консоли**
   - Должно быть сообщение о версии

### ✅ ХОРОШАЯ ПРАКТИКА:

**Перед деплоем:**
```bash
# 1. Проверьте версию в version-check.js
grep "APP_VERSION" version-check.js
# Должно быть: const APP_VERSION = "1.9";

# 2. Проверьте версии в index.html
grep "version-check.js" index.html
grep "script.js" index.html
# Должны совпадать с APP_VERSION
```

---

## 🐛 Что делать если не работает

### Проблема: "Пользователи всё равно на старой версии"

**Решение:**
1. Проверьте, загрузили ли вы `version-check.js` на Netlify
2. Проверьте, совпадают ли версии в файлах
3. Попросите пользователя **принудительно обновить** (Ctrl+Shift+R)
4. Проверьте консоль на ошибки

### Проблема: "Бесконечная перезагрузка"

**Причина:** Версия в `version-check.js` не совпадает с версией в URL

**Решение:**
```html
<!-- Должны совпадать! -->
const APP_VERSION = "1.9";  ← в version-check.js
<script src="/version-check.js?v=1.9">  ← в index.html
```

### Проблема: "Ошибка в консоли"

Откройте F12 → Console и пришлите скриншот.

---

## 📊 Логи для отладки

Добавьте в консоли (F12):
```javascript
// Проверить текущую версию
localStorage.getItem('app_version')

// Сбросить версию (для теста)
localStorage.removeItem('app_version')
// Затем обновите страницу

// Посмотреть все в localStorage
console.table(localStorage)
```

---

## 🎯 Итого

**При каждом обновлении:**
1. ⬆️ Увеличить `APP_VERSION` в `version-check.js`
2. ⬆️ Увеличить версии в URL скриптов в `index.html`
3. 📤 Загрузить ВСЕ файлы на Netlify
4. ✅ Проверить в консоли

**Пользователи получат обновление:**
- Автоматически при следующем заходе
- Или при нажатии F5 (обновить страницу)

**НЕ нужно:**
- ❌ Просить очистить кэш вручную
- ❌ Ctrl+Shift+R
- ❌ Переустановка

**Всё происходит автоматически!** 🚀
